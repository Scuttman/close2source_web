> Deprecated

This file is deprecated. Please refer to the canonical documentation in `README.md` at the project root.

Why this file still exists:
* Direct deletion attempts inside the automated workflow did not remove it (likely tooling constraint).
* Keeping a tiny pointer avoids stale duplicate content and contributor confusion.

Action if you have repo rights:
1. Delete this file in a normal git workflow (git rm README_NEW.md && commit).
2. Ensure only `README.md` remains so GitHub renders the up‑to‑date docs.

Canonical docs include:
* Architecture & data model (plan, team, permissions)
* Multi‑assignee & modal UX
* Permission coupling rules
* Roadmap

— End —
